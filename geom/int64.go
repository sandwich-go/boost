// Code generated by tools. DO NOT EDIT.
package geom

import "strconv"

type PointInt64 struct {
	X, Y int64
}

// String returns a string representation of p like "(3,4)".
func (p PointInt64) String() string {
	return "(" + strconv.FormatInt(int64(p.X), 10) + "," + strconv.FormatInt(int64(p.Y), 10) + ")"
}

// Add returns the vector p+q.
func (p PointInt64) Add(q PointInt64) PointInt64 {
	return PointInt64{p.X + q.X, p.Y + q.Y}
}

// Sub returns the vector p-q.
func (p PointInt64) Sub(q PointInt64) PointInt64 {
	return PointInt64{p.X - q.X, p.Y - q.Y}
}

// Mul returns the vector p*k.
func (p PointInt64) Mul(k int64) PointInt64 {
	return PointInt64{p.X * k, p.Y * k}
}

// Div returns the vector p/k.
func (p PointInt64) Div(k int64) PointInt64 {
	return PointInt64{p.X / k, p.Y / k}
}

// In reports whether p is in r.
func (p PointInt64) In(r RectangleInt64) bool {
	return r.Min.X <= p.X && p.X <= r.Max.X && r.Min.Y <= p.Y && p.Y <= r.Max.Y
}

// Eq reports whether p and q are equal.
func (p PointInt64) Eq(q PointInt64) bool {
	return p == q
}

// ZPInt64 is the zero PointInt64.
var ZPInt64 PointInt64

// PtInt64 is shorthand for PointInt64.{X, Y}.
func PtInt64(X, Y int64) PointInt64 {
	return PointInt64{X, Y}
}

type RectangleInt64 struct {
	Min, Max PointInt64
}

// String returns a string representation of r like "(3,4)-(6,5)".
func (r RectangleInt64) String() string {
	return r.Min.String() + "-" + r.Max.String()
}

func (r RectangleInt64) RangePoints(with func(p PointInt64) bool) {
	if with == nil || r == ZRInt64 {
		return
	}
	for x := r.Min.X; x <= r.Max.X; x++ {
		for y := r.Min.Y; y <= r.Max.Y; y++ {
			if !with(PtInt64(x, y)) {
				return
			}
		}
	}
}

func (r RectangleInt64) RangePointsMinClosedMaxOpen(with func(p PointInt64) bool) {
	if with == nil || r == ZRInt64 {
		return
	}
	for x := r.Min.X + 1; x <= r.Max.X; x++ {
		for y := r.Min.Y + 1; y <= r.Max.Y; y++ {
			if !with(PtInt64(x, y)) {
				return
			}
		}
	}
}

func (r RectangleInt64) IntersectionWithLine(s PointInt64, e PointInt64) bool {
	if s.X <= r.Min.X && e.X <= r.Min.X || s.X >= r.Max.X && e.X >= r.Max.X || s.Y <= r.Min.Y && e.Y <= r.Min.Y || s.Y >= r.Max.Y && e.Y >= r.Max.Y {
		return false
	}
	a := s.Y - e.Y
	b := e.X - s.X
	c := e.Y*s.X - e.X*s.Y
	if ((a*r.Min.X+b*r.Min.Y+c)*(a*r.Max.X+b*r.Max.Y+c)) <= 0 || ((a*r.Max.X+b*r.Min.Y+c)*(a*r.Min.X+b*r.Max.Y+c)) <= 0 {
		return true
	}
	return false
}

func (r RectangleInt64) RangePointsMinMaxClosed(with func(p PointInt64) bool) {
	if with == nil || r == ZRInt64 {
		return
	}
	for x := r.Min.X + 1; x < r.Max.X; x++ {
		for y := r.Min.Y + 1; y < r.Max.Y; y++ {
			if !with(PtInt64(x, y)) {
				return
			}
		}
	}
}

func (r RectangleInt64) RangePointsMinOpenMaxClosed(with func(p PointInt64) bool) {
	if with == nil || r == ZRInt64 {
		return
	}
	for x := r.Min.X; x < r.Max.X; x++ {
		for y := r.Min.Y; y < r.Max.Y; y++ {
			if !with(PtInt64(x, y)) {
				return
			}
		}
	}
}

// Dx returns r's width.
func (r RectangleInt64) Dx() int64 {
	return r.Max.X - r.Min.X
}

// Dy returns r's height.
func (r RectangleInt64) Dy() int64 {
	return r.Max.Y - r.Min.Y
}

// Size returns r's width and height.
func (r RectangleInt64) Size() PointInt64 {
	return PointInt64{
		r.Max.X - r.Min.X,
		r.Max.Y - r.Min.Y,
	}
}

// Add returns the rectangle r translated by p.
func (r RectangleInt64) Add(p PointInt64) RectangleInt64 {
	return RectangleInt64{
		PointInt64{r.Min.X + p.X, r.Min.Y + p.Y},
		PointInt64{r.Max.X + p.X, r.Max.Y + p.Y},
	}
}

// Sub returns the rectangle r translated by -p.
func (r RectangleInt64) Sub(p PointInt64) RectangleInt64 {
	return RectangleInt64{
		PointInt64{r.Min.X - p.X, r.Min.Y - p.Y},
		PointInt64{r.Max.X - p.X, r.Max.Y - p.Y},
	}
}

// Inset returns the rectangle r inset by n, which may be negative. If either
// of r's dimensions is less than 2*n then an empty rectangle near the center
// of r will be returned.
func (r RectangleInt64) Inset(n int64) RectangleInt64 {
	if r.Dx() < 2*n {
		r.Min.X = (r.Min.X + r.Max.X) / 2
		r.Max.X = r.Min.X
	} else {
		r.Min.X += n
		r.Max.X -= n
	}
	if r.Dy() < 2*n {
		r.Min.Y = (r.Min.Y + r.Max.Y) / 2
		r.Max.Y = r.Min.Y
	} else {
		r.Min.Y += n
		r.Max.Y -= n
	}
	return r
}

// Intersect returns the largest rectangle contained by both r and s. If the
// two rectangles do not overlap then the zero rectangle will be returned.
func (r RectangleInt64) Intersect(s RectangleInt64) RectangleInt64 {
	if r.Min.X < s.Min.X {
		r.Min.X = s.Min.X
	}
	if r.Min.Y < s.Min.Y {
		r.Min.Y = s.Min.Y
	}
	if r.Max.X > s.Max.X {
		r.Max.X = s.Max.X
	}
	if r.Max.Y > s.Max.Y {
		r.Max.Y = s.Max.Y
	}
	// Letting r0 and s0 be the values of r and s at the time that the method
	// is called, this next line is equivalent to:
	//
	// if max(r0.Min.X, s0.Min.X) >= min(r0.Max.X, s0.Max.X) || likewiseForY { etc }
	if r.Empty() {
		return ZRInt64
	}
	return r
}

// Union returns the smallest rectangle that contains both r and s.
func (r RectangleInt64) Union(s RectangleInt64) RectangleInt64 {
	if r.Empty() {
		return s
	}
	if s.Empty() {
		return r
	}
	if r.Min.X > s.Min.X {
		r.Min.X = s.Min.X
	}
	if r.Min.Y > s.Min.Y {
		r.Min.Y = s.Min.Y
	}
	if r.Max.X < s.Max.X {
		r.Max.X = s.Max.X
	}
	if r.Max.Y < s.Max.Y {
		r.Max.Y = s.Max.Y
	}
	return r
}

// Empty reports whether the rectangle contains no points.
func (r RectangleInt64) Empty() bool {
	return r.Min.X >= r.Max.X || r.Min.Y >= r.Max.Y
}

// Eq reports whether r and s contain the same set of points. All empty
// rectangles are considered equal.
func (r RectangleInt64) Eq(s RectangleInt64) bool {
	return r == s || r.Empty() && s.Empty()
}

// Overlaps reports whether r and s have a non-empty intersection.
func (r RectangleInt64) Overlaps(s RectangleInt64) bool {
	return !r.Empty() && !s.Empty() &&
		r.Min.X < s.Max.X && s.Min.X < r.Max.X &&
		r.Min.Y < s.Max.Y && s.Min.Y < r.Max.Y
}

// In reports whether every point in r is in s.
func (r RectangleInt64) In(s RectangleInt64) bool {
	if r.Empty() {
		return true
	}
	// Note that r.Max is an exclusive bound for r, so that r.In(s)
	// does not require that r.Max.In(s).
	return s.Min.X <= r.Min.X && r.Max.X <= s.Max.X &&
		s.Min.Y <= r.Min.Y && r.Max.Y <= s.Max.Y
}

func (r RectangleInt64) Bounds() RectangleInt64 {
	return r
}

// Expanded returns a rectangle that has been expanded in the x-direction
// by margin.X, and in y-direction by margin.Y. The resulting rectangle may be empty.
func (r RectangleInt64) Expanded(margin PointInt64) RectangleInt64 {
	return RectangleInt64{
		PointInt64{r.Min.X - margin.X, r.Min.Y - margin.Y},
		PointInt64{r.Max.X + margin.X, r.Max.Y + margin.Y},
	}
}

func (r RectangleInt64) ExpandedByMargin(margin int64) RectangleInt64 {
	return r.Expanded(PtInt64(margin, margin))
}

// ZRInt64 is the zero RectangleInt64.
var ZRInt64 RectangleInt64

// RectInt64 is shorthand for RectangleInt64{Pt(x0, y0), Pt(x1, y1)}. The returned
// rectangle has minimum and maximum coordinates swapped if necessary so that
// it is well-formed.
func RectInt64(x0, y0, x1, y1 int64) RectangleInt64 {
	if x0 > x1 {
		x0, x1 = x1, x0
	}
	if y0 > y1 {
		y0, y1 = y1, y0
	}
	return RectangleInt64{PointInt64{x0, y0}, PointInt64{x1, y1}}
}

// RectInt64FromCenterSize constructs a rectangle with the given center and size.
// Both dimensions of size must be non-negative.
func RectInt64FromCenterSize(center, size PointInt64) RectangleInt64 {
	return RectInt64(center.X-size.X, center.Y-center.Y, center.X+size.X, center.Y+size.Y)
}

// HelixRectRangeFromCenterAndMarginInt64 由center节点逆时针螺旋由内向外访问margin区域内的所有节点
// 25   24   23   22   21
// 10    9    8    7   20
// 11    2    1    6   19
// 12    3    4    5   18
// 13   14   15   16   17
func HelixRectRangeFromCenterAndMarginInt64(center PointInt64, margin int64, with func(p PointInt64) bool) {
	var x, y, xNow, yNow int64
	xLen, yLen := int64(1), int64(1)
	rectXYLen := margin*2 + 1
	m, max := int64(0), rectXYLen*rectXYLen
	startX, startY := center.X, center.Y

	for m <= max {
		for x = startX; x >= startX-xLen; x-- {
			m++
			if m > max || !with(PtInt64(x, startY)) {
				return
			}
		}
		for y = startY - 1; y >= startY-yLen; y-- {
			m++
			if m > max || !with(PtInt64(x+1, y)) {
				return
			}
		}
		xLen++
		yLen++
		for xNow = x + 2; xNow <= x+xLen; xNow++ {
			m++
			if m > max || !with(PtInt64(xNow, y+1)) {
				return
			}
		}
		for yNow = y + 1; yNow <= y+yLen; yNow++ {
			m++
			if m > max || !with(PtInt64(xNow, yNow)) {
				return
			}
		}
		xLen++
		yLen++
		startX = xNow
		startY = yNow
	}
}
